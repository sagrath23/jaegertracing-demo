import { trace } from './traceFunctions'
import { mainSpan, mainTracer, traceClassFunction, traceClassMethod } from './traceClasses'

export {
  trace,
  mainSpan,
  mainTracer,
  traceClassFunction,
  traceClassMethod
}
